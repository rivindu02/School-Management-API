apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
  namespace: school-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
      - name: mongo
        image: mongo:7.0-jammy
        ports:
        - containerPort: 27017
          name: mongodb
          protocol: TCP
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: school-api-secret
              key: MONGO_INITDB_ROOT_USERNAME
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: school-api-secret
              key: MONGO_INITDB_ROOT_PASSWORD
        - name: MONGO_INITDB_DATABASE
          value: "school_db"
        volumeMounts:         # specifies the volumes to mount into the container's filesystem.
        - name: mongo-storage
          mountPath: /data/db   # The path within the container where the volume should be mounted
        - name: mongo-config    # to store configuration files and metadata
          mountPath: /data/configdb
        livenessProbe:  # Determines if the container is running and healthy--> if failed-> restart container
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: 60   
          periodSeconds: 30  # how often to perform the probe
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe: # Determines if the container is ready to accept traffic --> if failed-> pod removed from service endpoints but not restarted
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: 30
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 5
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "300m"
      volumes:
      - name: mongo-storage
        persistentVolumeClaim:
          claimName: mongo-pvc
      - name: mongo-config
        emptyDir: {}      # Creates temporary storage that exists only while the pod is running
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: school-api
spec:
  selector:
    app: mongo
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017
  type: ClusterIP
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: school-api-quota
  namespace: school-api
spec:
  hard:
    requests.cpu: "600m"
    requests.memory: "2Gi"
    limits.cpu: "1500m"
    limits.memory: "3Gi"
    persistentvolumeclaims: "5"
    requests.storage: "10Gi"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: school-api-limits
  namespace: school-api
spec:
  limits:
  - default:
      cpu: "100m"
      memory: "128Mi"
    defaultRequest:
      cpu: "50m"
      memory: "64Mi"
    max:
      cpu: "600m"
      memory: "1Gi"
    min:
      cpu: "10m"
      memory: "32Mi"
    type: Container
